# Next-Agent Prompt — Diamitani Music

Paste everything in the box below as your first message to a new agent
session (any platform — Cursor, Claude, ChatGPT, etc.) to resume this work
with full context. It's deliberately short; it points at the two files
that hold the depth so you don't have to paste a huge wall of text.

---

```
You're picking up work on the diamitani/Diamitani-Industries repo
(a static multi-domain site: diamitani.com + 5 subdomains, no build step,
no framework, Vercel Node serverless functions under /api, no external DB).

Before doing anything else, read these two files in the repo — they are
your full context, generated by the context-handoff skill:
  1. handoff/context-manifest.json — structured state: what's done, what's
     open, hard constraints, real links (commits/PR).
  2. handoff/TRANSCRIPT.md — the full narrative session record if you need
     the "why," not just the "what."

Current branch: cursor/diamitani-music-portfolio-company-17de
Open PR: https://github.com/diamitani/Diamitani-Industries/pull/1 (draft, → main)

One-paragraph summary of what exists already: Diamitani Music is a new
portfolio company (label.diamitani.com, /diamitani-music/) with a real
Vercel-serverless operating-model backend (roster, catalog, royalty/
recoupment calculator) under /api/diamitani-music/*. There's also a verified
multi-artist discography (Pat Dia + Patrick Diamitani, sourced directly from
Spotify's artist-embed API) surfaced at music.diamitani.com/artists/*, and a
reusable "catalog-track-critic" skill (skills/catalog-track-critic/SKILL.md)
that generates track descriptions — currently all flagged
confidence:"metadata-informed" because no audio-listening tool was available
when they were written. Do not present any of those as if they came from an
actual listen unless you re-run the skill with real audio-analysis access.

Read handoff/context-manifest.json → "openItems" for the prioritized list of
what's still outstanding (unconfirmed YouTube video IDs, unverified Pandora
presence, upgrading descriptions to "listened", deploy/DNS verification,
PR review). Read handoff/NEXT_STEPS_PROMPTS.md for ready-to-paste prompts
for each of those.

Hard constraints you must not violate (full list in the manifest):
  - Never invent a platform ID, release date, or acoustic detail.
  - Every vercel.json host rewrite must exclude 'api/' and 'assets/'.
  - No build step / no framework — stay plain static HTML + vanilla JS +
    the shared assets/theme.css design system.
  - /diamitani-music/ (the label) and /music/ (the personal artist page)
    are intentionally separate — don't merge them.
  - Branch names in this environment must match cursor/<name>-17de; don't
    force-push or merge/close PRs unless explicitly asked.

Ask me what to do next, or default to the highest-priority open item in
the manifest if I don't specify.
```

---

## If you're a human, not an agent

Same idea — open `handoff/context-manifest.json` for the structured state,
skim `handoff/TRANSCRIPT.md` for the story, and check
`handoff/NEXT_STEPS_PROMPTS.md` for ready-made prompts to hand to whichever
agent you're using next.
